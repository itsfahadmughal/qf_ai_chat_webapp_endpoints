# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chatdb?schema=public

# JWT
PORT=3000
JWT_SECRET=change-me

# Provider env fallbacks (leave blank if you’re BYOK-only)
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
PERPLEXITY_API_KEY=

OPENAI_BASE_URL=https://api.openai.com/v1
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
PERPLEXITY_BASE_URL=https://api.perplexity.ai

OPENAI_MODEL=gpt-4o-mini
DEEPSEEK_MODEL=deepseek-chat
PERPLEXITY_MODEL=sonar

# BYOK encryption master key (base64, 32 bytes)
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))" - Generates a secure key and paste below.
ENCRYPTION_KEY_BASE64=

# SMTP
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=SMTP_USERNAME
SMTP_PASS=SMTP_PASSWORD
SMTP_SECURE=false            # true for port 465, else false
SMTP_FROM="Qualityfriend Chat Ai <no-reply@qualityfriend.solutions>"

# Where frontend reset page lives:
FRONTEND_URL=http://localhost:5173
FRONTEND_RESET_PATH=/reset-password  # frontend route path
